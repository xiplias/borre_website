{
  "name": "borre-website",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run subfont",
    "serve": "vite preview",
    "prepare": "husky install",
    "lint": "prettier --write .",
    "subfont": "subfont --debug --output=dist dist/index.html"
  },
  "dependencies": {
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "husky": "^7.0.2",
    "prettier": "^2.4.0",
    "pretty-quick": "^3.1.1",
    "subfont": "^6.2.1",
    "typescript": "^4.3.2",
    "vite": "^2.5.4"
  }
}
